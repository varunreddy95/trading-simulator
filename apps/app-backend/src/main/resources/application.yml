spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/papertrading
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: paper-trading-backend

server:
  port: 8080
