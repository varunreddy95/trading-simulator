spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/papertrading
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

  alphavantage:
    api-key: 8P2EPHQTM5Y3KT77
    base-url: https://www.alphavantage.co

  management:
    endpoints:
      web:
        exposure:
          include: health, info, rate-limit

